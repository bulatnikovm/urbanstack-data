-- Зв'язка тег × задача. Джерело: dim9000_fast_eu.task_tags_links.
--
-- Грануляція — ЗАДАЧА, не заявка: у CRM тег вішається на задачу, і вже
-- через `order_tasks.order_id` він доходить до заявки (це робить
-- int_order_tags). Тут — тільки чистий переклад назв колонок.
--
-- ⚠️ Сурогатного ключа в таблиці немає: рівно дві колонки, і ключ —
-- пара (tag_id, task_id). Перевірено, що вона унікальна: 6 938 рядків =
-- 6 938 різних пар.

select
    tag_id,
    task_id
from {{ source('dim9000_fast_eu', 'task_tags_links') }}
