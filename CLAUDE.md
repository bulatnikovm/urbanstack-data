# CLAUDE.md — UrbanStack Data Infrastructure

> Знімок стану на 2026-07-08. Живий документ — онови секції 4, 5, 8, 9 по мірі просування по фазах.

## 1. Хто я і що роблю

Микита — єдина людина в аналітиці/даних в UrbanStack (раніше DIM9000), proptech, ~30 000 мешканців, 150+ будинків, Україна. Самостійно побудував весь стек: BigQuery + Python ETL + Looker Studio. Зараз переводжу інфраструктуру на dbt і будую новий продуктовий дашборд на React (Bklit UI + shadcn/ui).

Є три дашборди: **продуктовий** (буде тільки внутрішній, не white-label), **операційний** та **фінансовий** (обидва підуть у white-label для майбутніх клієнтів UrbanStack).

## 2. Ціль поточної роботи — два треки

- **Тренувальний трек (зараз тут):** навести лад у поточній (single-tenant) BigQuery-базі UrbanStack — dbt-моделі, чистий реєстр метрик, новий продуктовий дашборд. Це не пісочниця — реальні продакшн-дані, реальні баги, реальна користь для UrbanStack просто зараз.
- **Цільовий трек (пізніше):** перенести здобуті уроки в дизайн мультитенантної бази для UrbanStack SaaS (1+ ЖК/УК, white-label клієнти).

## 3. BigQuery — інфраструктура

Project: `analytics-454817`

| Датасет | Статус | Що це |
|---|---|---|
| `postgresqldim9000` | **активний** | Stitch-sync продуктової Postgres БД: CRM (users/spaces/sections/houses/complexes), білінг (master_buh_*), копія Amplitude events (`EVENTS_407641`), + вже існуючі "недо-марти" (`dm_*`, `vw_dm_*`, `view_*`) |
| `finance_dash` | активний | Фінансовий шар. Вже має конвенцію `dim_*` / `fact_*` / `mart_*` — хороший приклад для dbt-міграції, переносити майже 1:1 |
| `dim9000_fast` | активний (**US**) | Окремий внутрішній продукт (алертна система), **не джерело жодного дашборду** (0 Looker-задань). Аудит не робимо. План: перенести в EU. |
| `dim9000_analytics` | активний (**US**) | Окремий внутрішній продукт (masterbuh), **не джерело дашбордів** (0 Looker-задань). Аудит не робимо. План: перенести в EU. |
| `bigquery` | **осиротілий (US)** | Не було в снімку v0.1 (Микита його «жодного разу не відкривав»). Містить `EVENTS_407641` (ще одна US-копія Amplitude) + 9 ad-hoc event-view (Retention D7/14/28, Phone Auth, New Users…). **Жоден дашборд його не читає** (0 Looker-задань). Схема — `schema/us_bigquery_*`. |
| `amlitude_EU` | **не використовується** | Оригінальний Amplitude BigQuery export (`EVENTS_407641` + `users_objects_complexes`). Дублюючий і мертвий, як `postgresql` нижче — лишається в BigQuery, свідомо ігноруємо |
| `postgresql` | **МЕРТВИЙ** | Останній sync 2025-04-29 (підтверджено запитом). Не використовувати. Лишається в BigQuery, свідомо ігноруємо |

**Регіон датасетів (підтверджено 2026-07-08):** EU — `postgresqldim9000`, `finance_dash`, `amlitude_EU`; US — `dim9000_fast`, `dim9000_analytics`, `bigquery`. ⚠️ BigQuery **не джойнить між регіонами** — дашборди й Looker живуть у EU; US-датасети ізольовані. Для INFORMATION_SCHEMA використовуй `region-eu` (дашбордний шар) або `region-us` (внутрішні продукти) відповідно. `EVENTS_407641` існує у **трьох** копіях: `amlitude_EU`, `postgresqldim9000` (жива, її читає продуктовий дашборд), `bigquery` (US, осиротіла).

## 4. Знайдені проблеми (Фаза 0 audit)

1. **`deactivated_houses` drift — виявився ширшим, ніж у Фазі 0.** Початково: хардкод-список задубльований у 4+ product views з розбіжністю (10 записів у `view_monthly_active_residents`/`vw_dm_complex_user_segments_monthly`, 11 у `vw_dm_apartment_occupancy_monthly`/`vw_dm_operations_monthly`, лише **3 з 11** у `vw_dm_objects_filter`). Аудит операційного дашборду (Фаза 1) показав ще гірше: у Looker custom SQL співіснують **4 різні механізми** виключення домів/ЖК одночасно — (a) статичний список 10 UUID, (b) повна відсутність фільтра у 2 запитах, (c) окремий список 3 ЖК-UUID спільний із продуктовою стороною, (d) окремий рядковий механізм по назві ЖК+номеру будинку лише в одному запиті (реальні адреси підтверджено: Варшавський=вул.Крістерів, Севен=Дніпровська набережна, Ліпінка=вул.Замковецька). **Підтверджено прямим читанням `vw_dm_operations_monthly`:** канонічний механізм — `house_deactivation_dates` (CTE з реальними датами деактивації, **11** UUID), і в ньому є будинок (`f94470ce…`, деактивовано 2025-12-01), якого **немає** в жодному з 10-записних хардкод-списків operational Looker SQL — точне підтвердження дрейфу, не здогадка. ✅ буд.108 (вул.Замковецька, ЖК "Ліпінка") — беклог позначав як "тест деактивації" (не фінально), але Микита підтвердив (2026-07-09): будинок деактивовано, виключення в `custom_occupancy__8677b241.sql` коректне. **Рішення (Микита, 2026-07-09): фікс структурної проблеми (4 механізми виключення) відкладено до dbt-фази** — проміжний `ref_deactivated_houses` view робити не будемо. Деталі — `looker_extracted/operational/AUDIT.md`.
2. ✅ **Переосмислено після аудиту фінансового дашборду (2026-07-09).** Це не "дві конфліктуючі методики без рішення" — це **міграція в процесі**, задокументована прямо в коді views (коментарі `>>> ЗМІНА`, `★ НОВЕ ПОЛЕ`): методика Аліони (`debt_balance − paid_amount`) вже прийнята як нова канонічна (`mart_debt_alena`, і `mart_debt_aging` — обидва явно позначені "за НОВОЮ формулою"). `mart_debt_flat` навмисно тримає обидві цифри поруч для перехідного порівняння. ⚠️ **Реальна неузгодженість:** `mart_debt_years` (аналогічний до `mart_debt_aging` розподіл боргу, тільки по роках) **не перейшов** на методику Аліони — досі на сирому `debt_balance`. Плюс FIN-006 ("Дисциплінований платник") виявився частково реалізованим: стара same-month методика в `mart_payment_rates.is_natural` + нова "формула Артема" (когортна, X→X+1) в `mart_payment_rates_cohort.is_natural`, з прямою вказівкою в коді перейти на другу. Деталі — `looker_extracted/financial/AUDIT.md`.
3. ✅ **Закрито (реєстр v0.2).** `mart_payment_rates` vs `mart_payments_rate` — `mart_payment_rates` (grain: приміщення × послуга) **актуальний**, `mart_payments_rate` (grain: ЖК × місяць) — **legacy**. FIN-005 у реєстрі виправлено на `mart_payment_rates`. Ризик плутанини через схожі назви лишається — тримати в голові при пошуку.
4. ✅ **Закрито повністю (Фаза 1).** Прихована логіка Looker (Стр.2-5) відновлена з BigQuery job history — див. `looker_extracted/`. **Усі** джерела даних Looker виявились custom SQL (не таблиці): продуктовий дашборд (`report_id c2180c98`) = 27 джерел, 23 custom на `EVENTS_407641` (активація/ретеншен/core-events/phone-auth). Прив'язка sql↔сторінка підтверджена (PDF-експорт + Микита як автор дашборду, не Артем — питання про *вміст дашборду* адресуються Микиті, Артем лише методолог бізнес-визначень). Деталі — `looker_extracted/product/_index.md`.
5. ✅ **Закрито:** `iban` у джерелі `dim_llc` (`tascombank_merchants WHERE purpose='payment'`) унікальний — перевірено запитом, fan-out ризику при джойнах немає.
6. ✅ **Закрито:** `postgresql` емпірично підтверджений мертвим (останній sync 2025-04-29 проти сьогоднішнього sync `postgresqldim9000`).
7. ✅ **Закрито.** Рішення по `amlitude_EU`, `postgresql` і view `users_objects_complexes`: не мігрувати, не видаляти — лишаються в BigQuery як є, свідомо ігноруємо. Якщо десь у Looker щось досі на них посилається — не чіпати, поки не зʼявиться конкретна причина.
8. ✅ **Закрито (Фаза 1, аудит операційного дашборду).** Прочитано всі 18 Looker-джерел (`report_id 1a8ae601`) — див. `looker_extracted/operational/AUDIT.md`. Знахідки, окрім розширення бага #1 (п.1): словник перекладу категорій заявок задубльовано **тричі** з розбіжністю (`intercom_and_video`) — кандидат на dbt seed `dim_order_category`; третя (окрім двох продуктових) паралельна методика "confirmed/active user" знайдена в покинутому запиті — не критично, не на дашборді. Хардкод "Севен" (518/2100) — виявився в **застарілому, вже неживому** Looker-джерелі (`custom_active_users__5cd58443.sql`, Стр.1 замінено на `vw_dm_operations_monthly`) — не впливає на поточні цифри дашборду.
9. ✅ **Закрито (Фаза 1, крос-перевірка з "Backlog Дешбордів.xlsx").** Микита надав файл з авторським описом бізнес-логіки кожної метрики продуктового/операційного/фінансового дашбордів (лист "Технічний" і інвесторський звіт — поза скоупом). Дозволило підтвердити ~90% раніше знайденого SQL-мапінгу і закрити 5 unconfirmed operational-джерел (`d7ed1d25`, `e7906ec1`, `a4f3efef` — усі виявились живими, `2c8667f1` — імовірно застаріла ітерація). Знайдено 2 розбіжності документація↔SQL: (а) буд.108 "Ліпінка" — ✅ закрито, Микита підтвердив деактивацію (див. п.1); (б) **відкрито** — "Особові рахунки" на Стр.3 операційного мали б включати комерцію/паркінг/комори, а SQL рахує лише квартири — цифра (26 786) імовірно занижена, розбираємось. На продуктовій стороні підтверджено канонічну методику біометрії (Стр.5): `custom_phone_auth_3__63b36903.sql` — нова, повніша версія (3 метрики включно з "Fallback to PIN"), `custom_retention_3__b3c2e647.sql` — застаріла (2 метрики). Деталі — `looker_extracted/operational/_index.md` і `looker_extracted/product/_index.md`.

## 5. Реєстр метрик

✅ Конвертовано в git-friendly markdown: **`docs/metrics_registry.md`** (джерело правди). Оригінал `UrbanStack_metrics_registry.xlsx` збережено поруч як бекап.

Поточний стан **v1.3**: 13 Product, 7 Financial, 18 Operational = 38 метрик. Operational-розділ пройшов 3 ітерації уточнення (v1.0→v1.2, деталі — `looker_extracted/operational/_index.md`). v1.3 додав аудит фінансового дашборду (`looker_extracted/financial/AUDIT.md`) — переосмислено баг #2 (методика Аліони вже канонічна, знайдено конкретну неузгодженість `mart_debt_years`), FIN-006 виявився частково реалізованим.

Колонки: `ID` · `Домен` · `Метрика` · `Визначення` · `Формула/логіка` · `Grain` · `Джерело (dataset.view)` · `Сторінка дашборду` · `dbt target (план)` · `Власник методології` · `Статус` · `Нотатки/відомі проблеми`.

Статуси: `Active` / `Потребує аудиту` (логіка не знайдена в BQ) / `Потребує узгодження` (є конфліктні версії) / `Known issue` / `Заплановано` / `Готово, не візуалізовано`.

## 6. Продуктовий дашборд — відома lineage

| View | Grain | Ймовірна сторінка |
|---|---|---|
| `view_monthly_active_residents` | complex × month | Стр.1 (Потенційних/Підтверджені/Не власники) |
| `vw_dm_complex_user_segments_monthly` | complex × month | Стр.1 «Живі/Сонні/Мертві» — ✅ **вже на дашборді** (з'явилось після 2026-07-09) |
| `vw_dm_operations_monthly` | complex × month | Стр.1/4, найбільший view |
| `vw_dm_apartment_occupancy_monthly` | house × month | заселеність, не бачив на дашборді |
| `vw_dm_objects_filter` | complex × object_type × month | розбивка по типу об'єкта — **містить баг #1** |
| `dm_company_churn_monthly` | month (вся компанія) | churn — **графік був на дашборді, прибраний** (підтверджено Микитою); джерело лишилось підключеним у Looker |
| Стр.2, 3, 4, 5 | — | ✅ **витягнуто й прив'язка підтверджена** — `looker_extracted/product/` (report `c2180c98`, 27 джерел, 23 custom SQL). Метод і повна таблиця — `looker_extracted/product/_index.md`. |

⚠️ Таблиця вище описує стан на 2026-07-09. Актуальна повна карта дашборду
(5 сторінок × усі блоки), знайдені баги й проєкт dbt-моделей —
**`docs/product_domain_design.md`** (2026-08-04), див. §8б.

**report_id дашбордів у Looker Studio** (для майбутніх вилучень з job history): продуктовий = `c2180c98-0cf4-49af-a1d0-0ad3364cb599`, фінансовий = `ca96cfac-6fac-475f-b467-42ea4c4eaf6f`, операційний = `1a8ae601-9542-4198-be93-8ed41ca39d4f`.

## 7. Roadmap

| Фаза | Що | Статус |
|---|---|---|
| 0 | Аудит схеми, lineage, знайдені баги, реєстр v0.1 | ✅ закрито |
| 1 | Витягнути Looker custom SQL (Стр.2-5), аудит операційного дашборду, реєстр → v1.0 | ✅ **закрито** (2026-07-09) — Looker SQL витягнуто й прив'язку підтверджено, схема EU сдамплена, аудит операційного дашборду завершено, реєстр → v1.0 (29 метрик) |
| 2 | dbt на поточних даних: seeds → staging → intermediate → marts | ✅ **закрито 2026-08-04**: фінансовий (§8а) · операційний + опитування · продуктовий (§8б). Усі три домени в dbt, 224/224 тести зелені. Лишилось: автооновлення (service account) |
| 3 | Новий продуктовий дашборд: Bklit UI + shadcn/ui поверх чистих marts | 🔶 **в роботі** (2026-08-05) — дашборд живий на Vercel, 5 сторінок, Google-логін. Деталі — §8в |
| 4 | Перенос уроків у мультитенантний дизайн для UrbanStack SaaS | 🔲 |

## 8. Фаза 1 — підсумок (✅ закрито 2026-07-09)

- [x] Витягнути custom SQL Стр.2-5 — зроблено інакше, ніж планувалось: не з Looker UI, а з BigQuery job history (мітка `requestor=looker_studio`). Результат у `looker_extracted/`.
- [x] Дамп `INFORMATION_SCHEMA` (EU marts + US `bigquery`) → `schema/`.
- [x] Виправлено FIN-005 у реєстрі (`mart_payment_rates`).
- [x] `dim9000_fast` / `dim9000_analytics` — рішення: окремі внутрішні продукти, не джерела дашбордів, аудит не потрібен (план: перенести в EU).
- [x] Прив'язка sql↔сторінка для Стр.2-5 підтверджена (PDF-експорт + Микита).
- [x] Аудит операційного дашборду завершено — `looker_extracted/operational/AUDIT.md`. Фікс знахідок (баг #1 у ширшому вигляді, хардкод "Севен", дрейф словника категорій) свідомо відкладено до dbt-фази.
- [x] Реєстр метрик → **v1.0** (29 метрик, Operational-розділ заповнено).

**Наступний крок — Фаза 2 (dbt).** Відкриті технічні рішення, які треба прийняти на старті:
- `deactivated_houses`/`excluded_complexes`: гібрид — `houses.status='deactivated'` фільтр для нових деактивацій + перевірити, чи бекфілені 10 legacy-UUID (якщо ні — мінімальний seed лише для них).
- `dim_order_category`/`dim_order_type` seed — консолідувати 3 розбіжні копії словника (OPS-003).
- Уточнити в Микити контекст хардкоду "Севен" (518/2100) перед міграцією (OPS-006).
- Уніфікувати 3 паралельні методики "confirmed/active user" (продукт ×2 + operational, OPS-007) — можливо, разом з Артемом, якщо йдеться про канонічне бізнес-визначення.

## 8а. Фаза 2 — старт: dbt для фінансового домену (2026-08-03)

Мотивація: Микита будує payment scoring модель, потрібне чисте dbt-джерело замість прямого читання Looker/BigQuery views. Обрано фінансовий домен першим (найменший ризик, `finance_dash` уже мав чисту dim/fact/mart конвенцію — див. §9).

- [x] dbt-core 1.12.0 + dbt-bigquery встановлено (`dbt/.venv`), автентифікація — `gcloud auth application-default login` (`method: oauth`, без сервісного акаунту).
- [x] Проєкт `dbt/` — **пише в окремий датасет `dbt_finance`**, не чіпає продакшн `finance_dash`/Looker.
- [x] Повний ланцюжок сирі джерела (`postgresqldim9000.master_buh_service_payment`/`master_buh_service`/`master_buh_information`/`tascombank_merchants`/`operations`/`transactions` + геоієрархія `spaces→sections→houses→complexes`) → staging → `int_space_geo` (спільний, реюзати для product/operational) → fact/dim → mart. Кожна модель **звірена рядок-в-рядок** (COUNT+SUM) з відповідним `finance_dash`-view — 100% збіг.
- [x] **Фікс `mart_debt_years`**: оригінал рахував сирий `debt_balance`, тепер — методика Аліони (вирівняно з `mart_debt_aging`). Перевірено на реальних даних: production мав 1 "фантомний" рядок (борг = оплата, тобто вже сплачено) → 0 рядків після фіксу, як і очікувалось.
- [x] **FIN-006 перенесено з обома методиками явно**: `is_natural_same_month` (стара) і `is_natural_cohort` ("формула Артема", канонічна) — рішення користувача: не видаляти стару, показати обидві.
- [x] **Новий факт `fct_billing_monthly`** (grain: `space_id × billing_month`) — головний артефакт для payment scoring. Агрегує білінг/оплати/борг (обидві методики) з рівня space×service×month. Точково звірений вручну (сума по послугах = агрегат).
- [x] `tascombank_merchants.private_key` — підтверджено, ніде не вибирається (навіть у staging).
- ⚠️ `dim_space` **навмисно не фільтрує** деактивовані будинки/виключені ЖК (на відміну від product/operational) — борг за приміщенням у деактивованому будинку лишається реальним боргом. Рішення зафіксоване, не помилка.
- [x] ✅ **Методика боргу — остаточно вирішено (2026-08-03).** Звірка з PDF-експортом фінансового дашборду довела математично (точний збіг карточок "Дебіторка"/"Боржників усього"/"Борг 180+"): **методика Аліони — вже канонічна практика дашборду**, не відкрите питання. У `fct_billing_monthly` канонічні поля перейменовано на чисті імена без суфіксу (`debt_amount`/`is_debtor`/`debt_bucket`), стара методика лишається як `debt_balance_legacy`/`debt_bucket_legacy` — явно другорядна. Побічно підтверджено: `mart_payment_rates_cohort` ("формула Артема") теж реально на дашборді (карточка "Погашено боргів минулих періодів", точний збіг суми). Деталі — `looker_extracted/financial/AUDIT.md`.
- ⚠️ **Знайдено окремо (не про dbt):** липень 2026 в `mart_debt_flat` має лише 1 рядок замість десятків тисяч — білінг за місяць ще не синхронізувався. Варто перевірити зі сторони білінг-системи/Stitch, чому.

Деталі, структура моделей і команди запуску — `dbt/README.md`.

## 8б. Фаза 2 — продуктовий домен (✅ реалізовано 2026-08-04)

Статус: **21 модель + 2 seed'и в датасеті `dbt_product`, 224/224 тести зелені**
по всьому проєкту. Повний документ — **`docs/product_domain_design.md`** (карта
всіх 5 сторінок дашборду, знахідки, структура моделей, звірка, відкриті
питання).

Вхідні дані: свіжий PDF-експорт дашборду (дані на 2026-08-03) + 23 Looker
custom SQL з `looker_extracted/product/` + прямі перевірки на BigQuery.

**Факти про джерело:** `EVENTS_407641` — 13,4 млн рядків / 5,95 GB,
партиційована по `event_time` (DAY), кластеризована по `event_type`,
135 різних `event_type`. Колонка `user_id` (Amplitude identity) — **100% NULL**,
тому `user_properties.phone_number` — єдиний реальний ключ юзера. Join
`phone_number = users.phone` чистий: **13 925 з 13 928 (99,98%)** — головний
ризик домену знято. У даних є ~30 подій з датами до 2028 р. (клієнтський
годинник) — у staging потрібен guard `event_date <= current_date()`.

**Знахідки (нові, не було в аудиті Фази 1):**

1. 🔴 **`amplitude_id` — це установка додатку, не користувач.** 20 064 devices
   на 13 919 телефонів; 29,6% телефонів мають >1 `amplitude_id`. «Відвідувачі»
   на Стр.1 завищені на **22-25%** (лип.2026: 9 976 vs 8 180). На тій самій
   сторінці MAU% рахується вже по телефонах — два визначення поруч.
   ✅ **Рішення Микити (2026-08-04): переходимо на телефон** як канонічний ключ
   користувача. Історичний ряд «Відвідувачів» при цьому опуститься — прийнято.
2. 🔴 **Синтаксичний баг** у `custom_new_users__d303fc10.sql`: три `event_type`
   написані суміжними літералами без ком, BigQuery їх конкатенує
   (`SELECT 'a' 'b' 'c'` → `abc`). Метрика «41,2 год до цінної дії» рахується
   по 3 подіях замість 6.
3. 🔴 **Чотири різні списки core events** (4/3/5/10 подій) + «Голосування»
   визначене двома різними подіями — див. окремий блок нижче.
4. 🟡 **Мапінг модулів покриває 76% подій.** 32 з 135 `event_type` падають у
   'Інше' і викидаються з таблиці Стр.3 — серед них цілі модулі: Сповіщення
   (246k подій), Віджети (121k), Об'єкти (40k), Тимчасові доступи. Плюс блок
   `module_group` продубльовано в 2 запитах з розбіжністю
   (`'7. Профіль та Авторизація'` vs `'7. Профіль'`) — через це два графіки на
   одній сторінці не джойняться за назвою модуля.
5. 🟡 **`house_deactivation_dates` у `view_monthly_active_residents` — мертвий
   код.** CTE є, LEFT JOIN є, поле вибирається — `WHERE` по ньому немає.
   Тобто скоркарти Стр.1 деактивовані будинки фактично **не виключають**,
   попри вигляд коду. (Уточнення до §4 п.1: список канонічний, але в цьому
   view не застосований.)
6. 🟡 **П'ятий механізм виключення ЖК.** Product-запити виключають **3** ЖК
   (`09c50e9a…` DIM 9000, `b59820ce…` Форест, `585e7b5b…` БЦ «Арсенал»), а seed
   `test_complexes.csv` в operational — лише **1** (DIM 9000).
   ✅ **Закрито (Микита, 2026-08-04): «Форест» і БЦ «Арсенал» — це КЛІЄНТИ**,
   не тестові ЖК. Виключати їх було помилкою продуктових запитів. Канонічно —
   seed `test_complexes` (тільки DIM 9000), обидва домени тепер на ньому.
   Довідково: Форест — 1 будинок, 0 приміщень, 0 юзерів; БЦ «Арсенал» —
   1 будинок, 1 приміщення, 4 юзери; обидва створені 2025-08-06 13:01:49.

**Що на дашборді не продуктове:** два графіки платежів на Стр.3 — це
`transactions` (finance-домен, вже є `stg_finance__transactions`), дублювати не
треба. Заявки на Стр.3 рахуються **з Amplitude-подій**
(`request_new_desc_btn_create_tap`), а не з `orders` — це «намір у додатку»,
принципово інша метрика, ніж `fact_orders` в operational. Однаково називати не
можна. Заявок з БД на продуктовому дашборді немає взагалі.

**Нові графіки з часу аудиту (2026-07-09):** «Живі/Сонні/Мертві» на Стр.1
(= `vw_dm_complex_user_segments_monthly`, PROD-007 — реєстр ще каже «не
візуалізовано», треба оновити) і останній графік Стр.5 («Чистий відтік /
Сплячі / Аномально активні») — **джерело невідоме**, треба перевитягнути job
history за `c2180c98` після 2026-07-08.

**Архітектура (коротко):** окремий датасет `dbt_product`; одна staging-модель
`stg_amplitude__events` (incremental / `insert_overwrite`, партиція по
`event_date`, телефон хешується в `user_phone_sk` — сирий PII не виходить за
staging, щоб не ламати рішення в `stg_dim9000__users`); центральний seed
`product_event_catalog.csv` на 135 рядків (вбиває 4 списки core events і 2 CASE
по модулях — той самий прийом, що `dim_order_category` в operational) +
`product_module_catalog.csv` (назви + вікна відвалу 30/90/180 днів як дані, а
не хардкод); intermediate `int_user_identity` / `int_events_enriched` /
`int_user_sessions` / `int_user_monthly_activity`; 12 mart'ів по блоках
дашборду. `dim_calendar_month`/`dim_complex`/`dim_house` переюзуються з
`dbt_operational` крос-датасетно.

### Знахідки, що з'явилися вже під час реалізації

7. 🔴 **64 626 дублікатів у сирій таблиці подій** (0,48% рядків) — точні копії
   (той самий uuid/event_time/event_type/amplitude_id/session_id), артефакт
   повторної Stitch-синхронізації. Знайдено guard-тестом `unique(event_uuid)`,
   завищували всі підрахунки подій. `stg_amplitude__events` дедуплікує.
   Побічно: `event_id` — **не ключ** (34 430 унікальних на 13,4 млн рядків).
8. 🔴 **Дві події СКД не існують** — `widget_control_center_key_btn__click` і
   `widget_lock_key_btn__click` мають 0 записів за всю історію, при тому що
   фігурують у списку core-подій у STAR-запиті, `view_monthly_active_residents`
   і `vw_dm_complex_user_segments_monthly`.
9. 🔴 **У STAR два різні знаменники одночасно.** Поле `nsm_penetration_rate`
   всередині `custom_core_events__e242ceed.sql` ділить на ПІДТВЕРДЖЕНИХ
   (лип.2026: 15 854 → STAR ≈ 21%), а показник «STAR 10,03%» на Стр.4
   рахується в Looker проти рядка «Потенційних» (33 204 — усі юзери, включно з
   непідтвердженими). Тобто поле з самого STAR-запиту на дашборді **не
   використовується**. `mart_star_monthly` віддає обидва
   (`star_rate_of_confirmed` / `star_rate_of_potential`) — вибір за Артемом.
10. 🟡 **1 660 юзерів мають приміщення в кількох ЖК** (2 ЖК — 1 502, 3 — 131,
    4 — 21, 5-10 — 6). Скоркарти Стр.1 підсумовують per-complex значення, тому
    ці люди рахувались двічі (+1 604 до тоталу). ✅ **Закрито рішенням Микити
    2026-08-04: користувач рахується РІВНО ОДИН РАЗ** («два особові рахунки =
    один користувач» — напр. квартира у Варшавському 3 і комерція у
    Варшавському 2 = одна людина). Реалізовано через `primary_complex_id` в
    `int_user_exclusions`: пріоритет — житло важливіше за комерцію/паркінг,
    далі найстаріший будинок. Тепер сума по ЖК ДОРІВНЮЄ тоталу.

### ⚠️ Два незалежні механізми деактивації (перевірено 2026-08-04)

Деактивація БУДИНКУ і деактивація КОРИСТУВАЧА — **різні, не синхронізовані**
механізми. Поширена (і хибна) інтуїція: «якщо дім деактивували, у жителів має
стати `ROLE_INACTIVATED_CITIZEN`». У даних це не так:

| ЖК | Дім деактивовано | `ROLE_CITIZEN` | `ROLE_INACTIVATED_CITIZEN` |
|---|---|---|---|
| Варшавський Плюс (3 доми: 10, 19, 40/12) | **2026-08-01** | **1 496** | 22 |
| Севен (6 домів) | вер-жовт 2025 (рік тому) | **2 854** | 6 |

Тобто ролі не міняють ні одразу, ні через рік. `ROLE_INACTIVATED_CITIZEN`
(4 936 у `users`) — це індивідуальні деактивації; при цьому лише **579** з них
ще мають прив'язане приміщення, решті прив'язку зняли.

**Чому так:** УК свідомо НЕ відв'язує жителів деактивованого будинку від квартир
— щоб лишити можливість повернути їх (підтвердив Максим, 2026-08-04). Тобто в
даних НЕМАЄ жодного прапорця «цей житель більше не наш», і рахувати виключення
маємо ми самі.

**Рішення — спільна модель `int_user_exclusions`** (`models/intermediate/shared/`,
рішення Микити: «краще створити щось одне по деактивованих користувачах і
переюзати в дашбордах»). Грануляція `user_id × report_month`, чотири причини
виключення: `employee` / `test_complex` / `role_deactivated` / `house_deactivated`.
Виключення **похідне** від `houses.deactivated_at`, тому **оборотне автоматично**:
повернуть будинок — жителі повернуться в підрахунки без ручних правок, ніякого
seed'а зі списком людей вести не треба. Під нею — `int_user_space_links`
(user × house, власники ∪ мешканці).

**Наслідок:** фільтрувати треба ОБИДВА механізми. Тільки по ролі — пропустиш
~4,4 тис. людей з деактивованих домів. Тільки по будинку — 579 індивідуально
деактивованих.

⚠️ **Межа місяця:** будинок, деактивований 1 серпня, у СЕРПНІ вже не рахується
(рішення Микити). Порівнювати треба на рівні МІСЯЦЯ, не timestamp'а —
`deactivated_at` містить реальний час доби (`2026-08-01 11:59:36`), тому
наївне `> timestamp(report_month)` лишало б будинок активним цілий серпень.

Перевірка виключених по місяцях: серп.2025 — 742 (Варшавський), вер.2025 —
1 520 (+Севен 20-А/20-Б), жовт.2025 — 3 184 (+Севен 16-Г/16-Д/18-А/18-Б),
груд.2025 — 3 223 (+Ліпінка 108), **серп.2026 — 4 406** (+Варшавський Плюс).

✅ **Операційний домен переведено на цей механізм (2026-08-04).** Рішення
Микити: `statistic_citizen` — непрозорий агрегат джерела, невідомо як
рахується, довіряти не можна. Перевірка підтвердила конкретну проблему: він
**не виключає деактивовані будинки** — ЖК «Севен» пішов з УК вер-жовт 2025, а
снапшот показував 2 911 юзерів у квіт.2026 і 2 915 у серп.2026, назавжди.

Створено **`fct_users_monthly`** (grain `complex_id × report_month`) — мешканці
рахуються з `users` + прив'язок через `int_user_exclusions`.
`mart_monthly_complex_overview` тепер бере звідти; колонки снапшота лишились
поруч із суфіксом **`_src`** для звірки. `fact_user_space` теж підключений
(`is_active_resident` / `exclusion_reason` / `is_house_deactivated`).

Розклад різниці (лип.2026): снапшот 32 396 → **−1 700** подвійний рахунок людей
з приміщеннями в кількох ЖК → 30 696 → **−3 220** жителі деактивованих будинків
→ **−579** деактивовані за роллю → **26 895**. Севен: до деактивації
(серп.2025) 2 876 → після (лист.2025) **285** (лишився недеактивований
паркінг-будинок), снапшот увесь час ~2 913.

⚠️ Аналога `active_user` у `fct_users_monthly` свідомо немає — «активний
користувач» це продуктове визначення (core-подія за місяць), воно вже є в
`dbt_product.mart_user_base_monthly.active_core_mau` з явним списком подій із
seed'а. Другий підрахунок означав би повторити помилку, від якої тікаємо.

⚠️ Числа більше не збігаються з xlsx-звітом по користувачах — він будувався на
тому ж `statistic_citizen`. Збіг по БУДИНКАХ (110=110) не зачеплено.

✅ **`fact_user_space` теж виправлено (2026-08-04).** Він містив ВЛАСНУ логіку
прив'язки — лише `space_user`, без власників (`spaces.owner_id`), — при тому що
за призначенням це «аудиторія приміщень», а не «тільки не-власники». Дубль
логіки, ще й неповний. Тепер спільний `int_user_space_links` має атомарну
грануляцію `user_id × space_id` (був user × house), а `fact_user_space` —
тонка вибірка над ним. Буд. Змієнка 15/23: **75 → 424** (349 власників +
75 співмешканців). По базі: 37 941 рядок / 30 717 людей замість ~9,8 тис.
Продуктові тотали не змінились — product від початку об'єднував обидва способи.

**Ролі в `users` (35 897 усього):** ROLE_CITIZEN 30 413 · ROLE_INACTIVATED_CITIZEN
4 936 · співробітники (EMPLOYEE/MASTER/DISPATCHER/HOUSE_MANAGER/ADMIN та їх
INACTIVATED-версії) 548. З них прив'язку до приміщення мають 30 720, і серед них
співробітників лише 13.

### Звірка з дашбордом

- **Стр.2 (активація) — збіг.** Конверсія черв. 26,9% / трав. 18,5% / лип.
  23,9%; нові юзери лип. 376, черв. 390 — ті самі числа, що на графіку.
- **Стр.4 (STAR-категорії) — збіг з оригінальним SQL до ±1 юзера** (розбіжність
  рівно 1 = псевдо-юзер `'unknown phone_number'`). Виняток — «3. Голосування»
  (945 замість 640), це навмисна зміна.
- **Стр.1 (база) — розбіжність повністю розкладена:** PDF 33 213 → продакшн-view
  сьогодні 32 583 → **моя модель без point-in-time, сумою по ЖК: 32 587 (Δ=4)**
  → з point-in-time 29 101 → DISTINCT 27 497. Обидва відхилення свідомі.
- ⚠️ **Дрейф даних:** PDF (2026-08-03) не відтворюється навіть ОРИГІНАЛЬНИМИ
  запитами — усі числа сьогодні на 1-9% нижчі («1. Заявки» лип.: PDF 1 392,
  оригінальний SQL сьогодні 1 272). Причина не з'ясована, до dbt стосунку не
  має. Варто перевірити окремо.

### Нові модулі після переходу на seed (покриття подій 76% → 100%)

Лип.2026: **Сповіщення 3 448 юзерів (проникнення 45,8%)** — третій за
використанням модуль застосунку, якого на дашборді не було взагалі;
Об'єкти 1 179 (15,7%), Гардіан 634 (8,4%), Тимчасові доступи 479 (6,4%),
Віджети 389 (5,2%).

### Правило домену: core event = ТЕРМІНАЛЬНА дія

**Рішення Микити 2026-08-04.** Core event (він же цільова дія для STAR і
активації) — це **останній, завершальний івент ланцюжка**: не «подивився /
відкрив / поклацав», а «натиснув Проголосувати», «оплата пройшла», «заявку
створено». Реалізовано полем `is_core_event` у seed'і.

На практиці змінилась лише одна категорія — «3. Голосування». У додатку три
різні події навколо голосування:

| Подія | Унік. юзерів (лип.2026) | Подій | Подій на юзера |
|---|---|---|---|
| `vote_details_active_scrn__view` | 1 607 | — | побачив екран |
| `vote_details_active_form__tap` | 639 | 56 602 | **88,6** |
| `vote_details_active_btn_vote_tap` | 946 | 1 127 | **1,19** |

`_btn_vote_tap` поводиться як завершена дія (≈1 на юзера). `_form__tap` —
високочастотна взаємодія з формою (89 разів на юзера) **і при цьому дає менше
унікальних юзерів**, тобто це не проміжний крок воронки, а інша подія.

Наслідок: **на дашборді два різні числа за той самий місяць**. Стр.3 (воронка)
показує «Проголосували» = **946** (`_btn_vote_tap`), Стр.4 (STAR, категорія
«3. Голосування») показує **688** (`_form__tap`). Розбіжність ~27%.
Активація (Стр.2) використовує `_btn_vote_tap`, STAR і всі 10-подієві списки —
`_form__tap`.

`_btn_vote_tap` поводиться як завершена дія (≈1 подія на юзера), `_form__tap` —
високочастотна возня у формі (88,6 на юзера) і при цьому дає МЕНШЕ унікальних
юзерів, тобто це не крок воронки, а інша подія. Після зміни Стр.3 і Стр.4
узгоджені (обидві ~945); раніше показували 946 і 688 за той самий місяць.

**Далі:** відкриті питання Q3-Q8 з `docs/product_domain_design.md` (Q1/Q2
закриті); автооновлення всіх трьох доменів (потрібен service account —
поточний oauth не годиться для unattended); Фаза 3 — дашборд на Bklit UI +
shadcn/ui поверх `dbt_product`.

## 8в. Фаза 3 — продуктовий дашборд (🔶 в роботі, старт 2026-08-05)

Живий на **https://urbanstack-dashboard.vercel.app**. Код — окремий репозиторій
`bulatnikovm/urbanstack-dashboard` (приватний); dbt лишився в
`bulatnikovm/urbanstack-data`. Локально дашборд живе в гілці
`claude/dashboard-api-planning-577b7a`, каталог `web/`, і виноситься в окреме
репо через `git subtree split --prefix=web`.

**Архітектура (свідомо проста, план — `docs/dashboard_plan.md`):**
`dbt marts → scripts/export-data.mjs → web/data/*.json (комітяться) → Next.js`.
Ні бази, ні API-сервера, ні cron: усіх даних 1.2 МБ, а `srv_*`-шар для
product/operational не потрібен — вони вже serving-розміру (< 5 МБ разом).
`srv_*` лишається тільки для фінансів (там мільйони рядків), Фаза E.

- [x] 5 сторінок за структурою старого Looker: Аудиторія / Активація /
      Залученість / STAR / Стан додатку (остання — заглушка, дані є).
- [x] Дейт-пікер: діапазон **місяців** в URL (`?from=&to=`), щоб період можна
      було переслати посиланням. Помісячний, бо всі марти помісячні.
- [x] Показуємо **поточний** місяць, включно з незавершеним (рішення Микити) +
      позначка «місяць триває · N з M днів».
- [x] Google-логін (Auth.js v5, allowlist в env, без бази), `proxy.ts` захищає
      сторінки до рендеру.
- [x] Bklit UI на пончиках, shadcn/ui на решті.

**Знахідки в даних, виправлені по ходу (не косметика):**
- пончик ОС сумував `active_users` по `mart_version_adoption` (грануляція
  міс×ОС×**версія**) — хто був на двох версіях, рахувався двічі (iOS 6 080
  замість 5 153). Новий агрегат `agg_os_monthly` — distinct по людині;
- «Без активності» на воронці активації — категорії **не існує**:
  `count_activated + count_passively_activated == count_new_users` тотожно;
- «Побачили голосування» → насправді `vote_details_active_scrn__view`,
  тобто **відкрили** екран, не побачили в стрічці — перейменовано;
- «Заявки, створені в додатку» → подія це **натискання кнопки**
  (`request_new_desc_btn_create_tap`), не факт створення — перейменовано;
- час до цінної дії: медіана замінена на **розподіл** (1 год / 1 день /
  7 днів) — медіана 0,2 год при P90 313 год не говорила ні про що;
- Looker-цифри ОС (Android 1630 / iOS 3362) виявились рахованими **лише по
  `news_scrn__view`** — одна подія. Реально активних 7 526.

**Дві пастки, задокументовані в `web/README.md` (легко наступити вдруге):**
- шрифт підключається через `--font-sans`, не `--font-geist-sans` — інакше
  `font-sans` не резолвиться й сторінка падає на серифний дефолт;
- `PieCenter.displayName` обовʼязковий: `isPieCenter()` шукає центр за
  `displayName` або `name`, а мініфікатор у **проді** перейменовує функції —
  центр пончика зникав мовчки, у деві не відтворювалось.

**Відкрито (початок наступної сесії):**
1. **`jobUser` для `dbt-runner`** — SA створений, права на датасети видані, але
   `roles/bigquery.jobUser` на рівні проєкту впав: у Микити `Editor`, а той
   свідомо не дає керувати IAM проєкту. Микита написав Артему.
2. **OAuth-клієнт у робочому проєкті** — зараз у особистому, тому екран згоди
   показує «Skriptly». Треба перестворити в `analytics-454817` (або новому
   робочому проєкті) з App name «UrbanStack Dashboard».
3. **Перевипуск секретів** — Client Secret і AUTH_SECRET засвітились у чаті.
4. **Vercel Hobby** — план для некомерційного використання; для робочого
   інструменту формально потрібен Pro.
5. **Автооновлення** (блокується п.1): 2:00 UTC Stitch→BigQuery, 6:00 GitHub
   Actions → `dbt build` → export → `git push` → Vercel редеплоїть сам.

## 9. Вже прийняті рішення

- Хардкод-списки (`excluded_complexes`, `deactivated_houses`, список "core events", `dim_order_category`/`dim_order_type`) → **dbt seeds**, не copy-paste між моделями — структурно унеможливлює баг #1 та його операційний аналог (словник категорій заявок, 3 копії з розбіжністю). **Рішення 2026-07-09: жодного проміжного BigQuery view (`ref_deactivated_houses`) до dbt не робимо** — фікс лише в dbt-фазі, щоб не переробляти двічі.
  - **Підтверджено Микитою:** `houses.status = 'deactivated'` — надійне поле, але **тільки для домів, деактивованих після появи цього поля**. Для 10 хардкод-домів дату деактивації Микита виставляв вручну заднім числом, бо поля тоді не існувало — вони можуть НЕ мати `status='deactivated'` зараз (ретроактивно не бекфілили). План для dbt: перевірити SQL-запитом, чи всі 10 UUID мають цей статус зараз; якщо ні — гібрид (`status`-фільтр для нових + невеликий seed лише для legacy-домів, а не повний список).
- Спільний intermediate `int_space_geo` для ланцюжка `spaces → sections → houses → complexes`, який зараз повторюється практично в кожному запиті (і в finance_dash, і в продуктових views, і в операційних Looker custom SQL).
- ЖК "Севен" має захардкожені числа (518/2100 юзерів за вер-жовт 2025) — знайдено в `custom_active_users__5cd58443.sql`, але це джерело вже **застаріле** (Стр.1 операційного замінено на `vw_dm_operations_monthly`, без хардкоду). Не блокує dbt, але контекст (чому взагалі знадобився патч) варто зберегти, якщо спливе деінде.
- `finance_dash` переноситься в dbt майже 1:1 (найменший ризик, хороший перший PR — вже має правильну dim/fact/mart конвенцію).
- Продуктова сторона (`vw_dm_*` монолiти) потребує повного рефакторингу в `fact_user_lifecycle` / `fact_activation_events` / `fact_module_usage`.
- Фаза 3: **Bklit UI + shadcn/ui разом**, не тільки Bklit — взяти найкраще з обох.
- **Спільний механізм виключення користувачів — `int_user_exclusions`** (рішення 2026-08-04). Один артефакт для всіх дашбордів замість фільтрів, розсипаних по домену. Похідний від `houses.deactivated_at` + ролі → оборотний автоматично. Чиста база = `is_active_resident`. Деталі — §8б.
- **Користувач рахується один раз** (рішення 2026-08-04): кілька особових рахунків / приміщень у різних ЖК = одна людина. Розріз по ЖК — через `primary_complex_id` (житло > комерція, далі найстаріший будинок).
- **Канонічний ключ користувача в продуктовому домені — телефон, не `amplitude_id`** (рішення 2026-08-04). `amplitude_id` = установка додатку, завищує «Відвідувачів» на 22-25%. У моделях ходить як `device_id` — лише для метрик установок/версій. Приймаємо, що історичний ряд «Відвідувачів» на дашборді опуститься. Сирий телефон не виходить за межі staging — далі скрізь `user_phone_sk` (SHA256), щоб не ламати рішення не тягнути PII у `stg_dim9000__users`.

## 10. Люди

- **Артем** — CEO/засновник, methodology owner для продуктових KPI (STAR, core events), приймає рішення по неоднозначних метриках.
- **Максим** — PM.
- **Аліона** — фінансові дані; її методика розрахунку боргу (`mart_debt_alena`) потребує узгодження зі старою (`mart_debt_flat`).
- **Микита** (я) — sole data/analytics lead, автор усього стеку.

**"Backlog Дешбордів.xlsx"** — авторська документація бізнес-логіки кожної метрики (окремі листи Продуктовий/Операційний/Фінансовий/Технічний/Аналітичний звіт по заявкам DIM для інвесторів). Джерело правди вищого рівня, ніж SQL, — використовувати для крос-перевірки при подальших аудитах чи dbt-міграції (напр. коли треба зрозуміти *намір*, а не тільки поточну реалізацію). Файл лежить у Микити локально (не в репо) — просити при потребі.

## 11. Корисні запити (regenerate schema snapshot)

```sql
-- Усі таблиці й view в проєкті (заміни region-eu на свій регіон)
SELECT table_catalog AS project, table_schema AS dataset, table_name, table_type
FROM `region-eu`.INFORMATION_SCHEMA.TABLES
WHERE table_catalog = 'analytics-454817'
ORDER BY dataset, table_name;
```

```sql
-- Всі колонки
SELECT table_schema AS dataset, table_name, column_name, data_type, ordinal_position
FROM `region-eu`.INFORMATION_SCHEMA.COLUMNS
WHERE table_catalog = 'analytics-454817'
ORDER BY dataset, table_name, ordinal_position;
```

```sql
-- SQL-визначення всіх view (lineage)
SELECT table_schema AS dataset, table_name, view_definition
FROM `region-eu`.INFORMATION_SCHEMA.VIEWS
WHERE table_catalog = 'analytics-454817'
ORDER BY dataset, table_name;
```

```sql
-- Перевірка "живості" датасету/таблиці
SELECT 'dataset_a' AS dataset, MAX(_sdc_received_at) AS last_synced
FROM `analytics-454817.dataset_a.table`
UNION ALL
SELECT 'dataset_b', MAX(_sdc_received_at)
FROM `analytics-454817.dataset_b.table`;
```

```sql
-- Регенерувати Looker custom SQL з історії завдань (як у Фазі 1).
-- Кожен джерело даних Looker = looker_studio_datasource_id. Looker обгортає
-- custom SQL у SELECT ... FROM (<custom>) — розгортання робить looker_extracted/_raw/extract_looker.py
SELECT
  (SELECT value FROM UNNEST(labels) WHERE key='looker_studio_report_id')     AS report_id,
  (SELECT value FROM UNNEST(labels) WHERE key='looker_studio_datasource_id') AS datasource_id,
  COUNT(*) AS runs,
  ANY_VALUE(query HAVING MAX LENGTH(query)) AS longest_query,
  ANY_VALUE(ARRAY(SELECT CONCAT(rt.dataset_id,'.',rt.table_id) FROM UNNEST(referenced_tables) rt)) AS refs
FROM `region-eu`.INFORMATION_SCHEMA.JOBS_BY_PROJECT
WHERE creation_time >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 90 DAY)
  AND job_type='QUERY' AND statement_type='SELECT' AND state='DONE' AND error_result IS NULL
  AND EXISTS (SELECT 1 FROM UNNEST(labels) WHERE key='requestor' AND value='looker_studio')
GROUP BY report_id, datasource_id;
```

**Сервісні аккаунти в job history** (щоб не плутати): `id-000-analytics-service-accou@analytics-454817.iam.gserviceaccount.com` — це **Stitch** (реплікація, тільки метадані-опити EVENTS_407641/primary keys), НЕ Looker. Запити дашбордів Looker ідуть під OAuth-аккаунтом власника (`nikitabulatnikov07@gmail.com`) і мітяться `requestor=looker_studio`. Ретенція JOBS_BY_PROJECT ~180 днів.
